# 更新日志

本文件记录 QuoteCue 的重要变更。

## [0.3.1] - 2026-08-31

### 新增

- 跨打开的标签页实时同步草稿修改、删除及发送确认后的清理，无需刷新页面。
- 离开未识别对话时将其草稿独立保留，需显式确认恢复后方可并入目标持久化对话。
- 官网新增自建 Umami 统计支持与标准 sitemap 端点。

### 变更

- 结合上下文证据解析文本锚点，提升回复内容变动后的锚点恢复准确率。
- 将宿主 composer action 状态与发送控件精确限定至活跃的 composer，避免全局选择器干扰。
- 延迟草稿加载状态反馈，避免快速读取和状态变更时的界面闪烁。

### 修复

- 在发送失败和重试失败时完整保留 composer 中的补充追问与批注草稿。
- 拒绝有歧义的发送确认，防止在匹配不确定时提前清空批注。
- 将安全输入框的编辑事件与状态严格保持在 iframe 内部，防止输入过程中焦点丢失或意外关闭。
- 稳定流式输出、语音输入切换及宿主动态样式调整期间的 composer 布局协调。
- 在已存储草稿存在不可读条目或触发存储限制被拒绝时，协调快照并允许显式确认清空与重试。
- 修复官网交互式演示在状态切换与示例切换时的编辑状态保留问题。

## [0.3.0] - 2026-08-24

### 新增

- 扩展与本地化官网新增日语支持。
- 上线 QuoteCue 官网，提供交互式演示、本地化产品说明和安装入口。
- 限制草稿容量，并在草稿连续 30 天未更新后自动过期，使本地存储占用保持可控。

### 变更

- 仅针对实际变化的内容执行消息扫描、composer 布局刷新和锚点投影，减少对宿主页面的影响。
- 新增 Chromium 扩展冒烟测试和生产 manifest 校验，提高发布验证覆盖度。

### 修复

- 保存或发送失败时保留待处理修改、重试上下文及无法读取的数据，同时按站点和对话隔离每次尝试。
- 更可靠地跟踪 SPA 导航与新消息，拒绝重复的消息标识和过期的对话状态。
- 提升宿主兼容性，包括 Claude 日语批注与稳定发送控件，以及 DeepSeek 助手消息的严格过滤。
- 发送确认后只清理对应的来源草稿，避免并发对话或重试删除无关内容。
- 修复依赖安全问题，并解决官网演示在 hydration、独立示例及发送/删除状态切换中的稳定性问题。

## [0.2.0] - 2026-07-23

### 新增

- 在 ChatGPT 之外新增 Claude、DeepSeek、Kimi 作为批注宿主，每个宿主都跟随自身主题的强调色。
- 批注删除支持撤销倒计时，可撤回操作。
- 新增覆盖层选择操作模式，作为原生选择菜单之外的另一种交互方式。
- 暴露无法解析的锚点和可恢复的发送状态，使匹配失败或发送失败后可以重试。

### 变更

- 抽象出通用 DOM host 引擎并统一发送控件，让新增宿主复用同一套集成路径，而不是各写一份宿主
  专属代码。
- 批量处理锚点投影，使大段回复的批注渲染更流畅。
- 将批注摘要改为无障碍 popover 组件。

### 修复

- 提升跨宿主发送确认的可靠性：事务化重放、匹配消息确认、乐观消息确认、空 composer 接管、
  换行重排处理、通过合成粘贴替换 composer 文本，以及 Kimi 上忽略空白差异的文本匹配。
- 草稿按对话隔离、隔离新对话的持久化逻辑，并对已存储的草稿数据进行版本化和校验，防止跨对话
  泄漏和数据损坏。
- 隔离批注输入事件并关闭 QuoteCue 的 shadow root，进一步加强页面隔离。
- 拒绝有歧义的文本匹配，正确锚定长选区和结构化选区。
- 修复大量批注编辑器与 UI 问题：编辑目标超出可视区域、空评论 UI、编辑器尺寸紧凑化、界面
  样式柔化、弹出框锚定、安全输入框焦点、脏数据关闭前的警告、悬浮详情、主题化的 tooltip、
  弹窗层级，以及被宿主覆盖层遮挡的徽标。
- i18n 兜底逻辑正确处理不受支持的宿主语言环境。
- 修复 WXT runner 依赖漏洞。

## [0.1.0] - 2026-07-22

首次发布。

- 支持对 ChatGPT 助手回复中的选中文本添加批注，并可附加评论。
- 在回复内容发生变化时，恢复文本锚点和批注高亮。
- 通过集成的 composer 界面创建、编辑、查看、删除和清空批注。
- 在本地持久化未完成的草稿，并按 ChatGPT 对话隔离。
- 将批注和已有的 composer 文本编译为一条聚焦的追问消息。
- 通过原生 ChatGPT composer 发送消息，并在发送被接受后清空草稿。
- 提供英文、简体中文和繁体中文界面。
- 将扩展的访问权限限制在本地存储和 `https://chatgpt.com/*`。
